services:
  onlyoffice-apps:
    ports:
      - "${EXTERNAL_PORT_HTTPS:-443}:443"
    environment:
      SSL_MODE: ${SSL_MODE}
      SSL_DOMAIN: ${SSL_DOMAIN}
      SSL_CERT_PATH: ${SSL_CERT_PATH}
      SSL_KEY_PATH: ${SSL_KEY_PATH}
      SSL_EMAIL: ${SSL_EMAIL}
      LETSENCRYPT_STAGING: ${LETSENCRYPT_STAGING:-false}
      LETSENCRYPT_FORCE_RENEW: ${LETSENCRYPT_FORCE_RENEW:-false}
      LETSENCRYPT_FAIL_OPEN: ${LETSENCRYPT_FAIL_OPEN:-false}
    volumes:
      # Only SSL_MODE=letsencrypt (a manual docker compose run) uses this; install-Docker.sh keeps its certificates in the host's /etc/letsencrypt and passes them as SSL_MODE=custom.
      - ./config/nginx/ssl/letsencrypt:/etc/letsencrypt
      - ./config/nginx/certs:/etc/nginx/certs:ro
